NODE_ENV=production
PORT=10000
PUBLIC_BACKEND_URL=https://YOUR-BACKEND.onrender.com
FRONTEND_URL=https://YOUR-FRONTEND.onrender.com
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=LuckyNumbervip_bot
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_CHANNEL_URL=https://t.me/YOUR_CHANNEL
ADMIN_TELEGRAM_ID=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Generate once and store only in Render Environment:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
DRAW_SECRET_ENCRYPTION_KEY=
# Generate separately (minimum 32 characters). It authorizes only the Render
# Cron Job request to close an overdue sales period; never place it in a URL.
SCHEDULER_SECRET=
TELEGRAM_INIT_DATA_MAX_AGE_SECONDS=86400
